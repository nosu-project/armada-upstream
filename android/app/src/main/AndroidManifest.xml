<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".ArmadaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Verified App Links: open https://armada.buzz links (invites,
                 shared channels) directly in the app. Verified against
                 https://armada.buzz/.well-known/assetlinks.json (shipped in
                 client/public/), which must list the APK signing cert's
                 SHA-256 fingerprint. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="armada.buzz" />
            </intent-filter>

            <!-- Share target: receive text/links/files from other apps' share
                 sheets (ShareTargetPlugin stages the payload for the web
                 layer's destination picker). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Sharing Shortcuts (API 29+): the static share-target category
                 that dynamic conversation shortcuts must carry to surface as
                 Direct Share suggestions in the share sheet. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
            <!-- Direct Share back-compat (API 24-28) via androidx.sharetarget:
                 serves the same dynamic shortcuts through the legacy
                 ChooserTargetService API. -->
            <meta-data
                android:name="android.service.chooser.chooser_target_service"
                android:value="androidx.sharetarget.ChooserTargetServiceCompat" />

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Background notification relay: a foreground service that holds a
             persistent Nostr REQ subscription to the relay(s) and posts native
             notifications the instant a matching event arrives. No FCM/Google. -->
        <service
            android:name=".NotificationRelayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- A persistent relay connection is not a batch "data sync": using
                 dataSync subjects the service to Android 15's 6h/24h cap (it
                 gets killed mid-session) and Android 14's ban on starting it
                 from boot. specialUse is the category for exactly this "doesn't
                 fit the other buckets" case and is exempt from both. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Hold a persistent connection to the user's Nostr relays to deliver message notifications" />
        </service>

        <!-- Restarts the notification service after reboot or app update. Not
             exported: BOOT_COMPLETED / MY_PACKAGE_REPLACED are system
             broadcasts and are delivered regardless. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Bluetooth mesh (bitchat) foreground service: keeps the BLE mesh
             advertising/scanning while the app is backgrounded. -->
        <service
            android:name=".MeshForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Ongoing voice call: the persistent call notification, and the
             foreground state that keeps a backgrounded call connected and
             audible. `microphone` is what lets capture continue off screen
             (Android 11+ silences a backgrounded app's mic without it);
             `mediaPlayback` is the floor the service falls back to while
             RECORD_AUDIO is still ungranted, since calls are joined muted. -->
        <service
            android:name=".CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone|mediaPlayback" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Restart the notification service after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Voice/video calls (LiveKit via WebView getUserMedia). Capacitor's
         BridgeWebChromeClient grants the WebView's onPermissionRequest for
         audio/video capture only if the matching Android permission is
         declared here (it also drives the runtime prompt). Without
         RECORD_AUDIO the mic is silently denied and voice never works. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The ongoing-call foreground service's two types. Without it the
         backgrounded process is a cached one Android 12+ freezes within
         seconds, which drops the call. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Let the user exempt Armada from battery optimization so the persistent
         relay connections survive Doze and the service can restart on boot
         (Android 15+). Only requested when background notifications are on. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Bluetooth mesh (bitchat) permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <!-- Mic/camera are used for calls but not required to install/run. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
